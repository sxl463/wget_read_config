wgetrc is the "main" initialization file, it is convenient to have a special facility for storing the configuration information.

Wget reads file wgetrc (under /etc) upon startup, recognizing a limited set of commands.


